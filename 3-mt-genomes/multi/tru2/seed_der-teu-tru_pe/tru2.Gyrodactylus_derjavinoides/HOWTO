Check for circularity.

```bash
~/src/MITObim/MITObim/misc_scripts/circules.py -f <(cat iteration4/temp_baitfile.fasta |grep "der" -A 1) -k 31
```

mt genome looks reasonably complete but low coverage. No circularity found.
